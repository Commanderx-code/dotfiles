# ---------------------------------------------------------
# Main Fish Config (minimal — modularized via conf.d/)
# ---------------------------------------------------------

# Login shell greeting
if status is-login
    echo "⚡ Welcome back, commander"
end
