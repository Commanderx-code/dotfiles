# Install Batch 1

This archive contains documentation only; it does not replace your Fish or Neovim source configs.

From `~/dotfiles`, extract the archive so its `docs/` tree merges into your existing Bible. Review with `git status --short`, stage the docs, then use `config-index fish` or `config-index neovim`.
