function fastfetch --wraps fastfetch --description 'System information with the original boxed layout'
    # Use the original config unchanged, including its side logo and boxes.
    command fastfetch $argv
end
